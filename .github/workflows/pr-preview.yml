name: PR Preview

on:
  pull_request:
    branches:
      - main

jobs:
  build:
    name: Build (PR)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # This is a static HTML site, so there's nothing to compile.
      # If you later add a real build step (npm run build or similar)
      # you can put it here.
      - name: Validate repository structure
        run: |
          echo "Static site – no build step required."
          ls -R
